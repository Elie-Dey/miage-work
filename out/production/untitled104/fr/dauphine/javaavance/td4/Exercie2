

Resultat : vrai
            faux

 Comme nous somme dans un hashmap il faut redefinir equals et hashmap dans la classe point

 public boolean equals(Objet o){
            if (this == o) return true;
            if (!(o instanceof Point)) return false;
            Point point = (Point) o;
            return getX() == point.getX() && getY() == point.getY();
         }

      public int hashcode(){
            return Integer.rotateLeft(Y,16);
            }